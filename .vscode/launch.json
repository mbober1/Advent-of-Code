{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch debug",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/bin/AoC",
      "stopAtEntry": true,
      "cwd": "${workspaceFolder}",
      "linux": {
        "MIMode": "gdb",
        "miDebuggerPath": "/usr/bin/gdb"
      },
      "preLaunchTask": "build",
    }
    
  ]
}